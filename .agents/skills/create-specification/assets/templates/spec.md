# {{title}}

## Problem Statement

The problem, from the user's point of view.

## Solution

The solution, from the user's point of view.

## User Stories

A numbered list that covers each aspect:

1. As an {{actor}}, I want {{feature}}, so that {{benefit}}.

## Implementation Decisions

The decisions made: the modules built or changed, the architecture choices, the schema changes, and the specific interactions. Write the interfaces at the seams that this spec adds or changes as real signatures, type shapes, or API contracts. Do not describe them in prose. Where the change crosses or creates a module boundary, state which call paths change.

## Dependencies

The new libraries or services that this spec adds, each with the reason that it earns its place — or "None". An implementation session does not add a dependency that this section does not name.

## Testing Decisions

The seams under test (from step 2). What makes a good test here (external behavior only). Worked examples — concrete input → expected output pairs — for behavior that computes something. Prior art: similar tests that exist in the codebase.

## Out of Scope

What this spec excludes on purpose.

## Further Notes

Anything else that the implementer needs.
